<%= form_for :project, url: url, method: verb do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Nombre' %>
    <%= form.text_field :name, id: :project_name %>
  </div>

  <div class="field">
    <%= form.label :description, 'Descripción' %>
    <%= form.text_area :description, id: :project_description %>
  </div>

  <div class="field">
    <%= form.label :score, 'KM' %>
    <%= form.number_field :score, id: :project_score %>
  </div>

  <div class="field">
    <%= form.label :status, 'Estado' %>
    <%= form.number_field :status, id: :project_status %>
  </div>
  <%= form.hidden_field :programme_id, value: @programme.id %>
  <div class="actions">
    <%= form.submit 'Guardar' %>
  </div>
<% end %>
