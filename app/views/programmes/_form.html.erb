<%= form_with(model: programme, local: true) do |form| %>
  <% if programme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(programme.errors.count, "error") %> prohibited this programme from being saved:</h2>

      <ul>
      <% programme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Nombre' %>
    <%= form.text_field :name, id: :programme_name %>
  </div>

  <div class="field">
    <%= form.label :description, 'Descripción' %>
    <%= form.text_area :description, id: :programme_description %>
  </div>

  <div class="field">
    <%= form.label :score, 'KM' %>
    <%= form.number_field :score, id: :programme_score %>
  </div>

  <div class="field">
    <%= form.label :status, 'Estado' %>
    <%= form.number_field :status, id: :programme_status %>
  </div>

  <div class="actions">
    <%= form.submit 'Guardar' %>
  </div>
<% end %>
